# Calliope repository record

- Product name: **Calliope**
- Public repository target: https://github.com/emmaGH1/calliope
- Local workspace directory: `calliope/` (the containing workspace currently has an external file lock, so the product/runtime rename is applied without moving this directory)
- Default branch: `main`
- License: MIT
- Remote status at setup: repository exists, public, and had no branch content; no remote content was overwritten

## History policy

The initial implementation was developed under the temporary working names
Calliope and Calliope. Existing commits retain those names as provenance. New
product-facing code, documentation, package metadata, and milestone commits use
Calliope. The rename changes branding and runtime identifiers; Sibyl's official
API names and the `charter` memory category remain unchanged because they are
part of the data model and external integration contract.

## Submission intent

This repository is the public artifact for the Sibyl Labs Hackathon. It contains
the verified local prototype, the six-page product site and console, the deletion test, the honest ACP/LLM
integration seams, and the evidence documentation needed for judging.
